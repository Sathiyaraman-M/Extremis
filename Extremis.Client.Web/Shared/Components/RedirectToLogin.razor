@code {
    protected override void OnInitialized()
    {
        var redirectUrl = $"authentication/login?returnUrl={Uri.EscapeDataString(NavigationManager.Uri)}";
        NavigationManager.NavigateToLogin(redirectUrl);
    }
}